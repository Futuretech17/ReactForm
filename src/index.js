import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

// challenge: Make the content typed in the form come after the hello in the heading
// 1. How to capture what is input by user.
// 2. How to update it after the submit button is clicked.
